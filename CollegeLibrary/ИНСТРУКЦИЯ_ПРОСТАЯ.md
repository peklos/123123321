# БИБЛИОТЕКА С АВТОРИЗАЦИЕЙ - ПОШАГОВАЯ ИНСТРУКЦИЯ

## ЧТО ДЕЛАЕТ ПРИЛОЖЕНИЕ:

1. Сначала окно ВХОДА (логин + пароль)
2. Можно зарегистрироваться или войти
3. После входа - окно с КНИГАМИ (добавить/удалить)

```
┌─────────────────────┐         ┌────────────────────────────┐
│   ВХОД В СИСТЕМУ    │         │  БИБЛИОТЕКА - Иван         │
│                     │         │                            │
│  Логин: [_____]     │  ──►    │  [Название книги] [Добав]  │
│  Пароль: [_____]    │         │  ┌──────────────┐  [Удал]  │
│                     │         │  │ Книга 1      │          │
│  [Войти] [Регистр]  │         │  │ Книга 2      │          │
└─────────────────────┘         │  └──────────────┘          │
                                └────────────────────────────┘
```

---

## ШАГ 1: Создаём проект

1. Открыть **Visual Studio**
2. **Создать проект** → ищем **Windows Forms App**
3. Называем **CollegeLibrary** → Создать

---

## ШАГ 2: Добавляем файлы

В проекте нужно 3 файла:
- `Program.cs` - точка запуска
- `LoginForm.cs` - форма входа
- `BooksForm.cs` - форма с книгами

**Как добавить файл:**
Правой кнопкой по проекту → Добавить → Класс → Ввести имя

---

## ШАГ 3: Program.cs (точка запуска)

```csharp
using System;
using System.Windows.Forms;

namespace CollegeLibrary
{
    static class Program
    {
        [STAThread]
        static void Main()
        {
            Application.EnableVisualStyles();
            Application.Run(new LoginForm());
        }
    }
}
```

---

## ШАГ 4: LoginForm.cs (форма входа)

```csharp
using System;
using System.Collections.Generic;
using System.Drawing;
using System.IO;
using System.Text.Json;
using System.Windows.Forms;

namespace CollegeLibrary
{
    public class LoginForm : Form
    {
        private TextBox txtLogin;
        private TextBox txtPassword;
        private Button btnLogin;
        private Button btnRegister;

        // Словарь пользователей (логин → пароль)
        private Dictionary<string, string> users = new Dictionary<string, string>();
        private string usersFile = "users.json";

        public LoginForm()
        {
            CreateElements();
            LoadUsers();
        }

        private void CreateElements()
        {
            this.Text = "Вход в библиотеку";
            this.Size = new Size(300, 200);

            // Логин
            Label lblLogin = new Label();
            lblLogin.Text = "Логин:";
            lblLogin.Location = new Point(20, 20);
            this.Controls.Add(lblLogin);

            txtLogin = new TextBox();
            txtLogin.Location = new Point(100, 20);
            txtLogin.Size = new Size(150, 25);
            this.Controls.Add(txtLogin);

            // Пароль
            Label lblPass = new Label();
            lblPass.Text = "Пароль:";
            lblPass.Location = new Point(20, 55);
            this.Controls.Add(lblPass);

            txtPassword = new TextBox();
            txtPassword.Location = new Point(100, 55);
            txtPassword.Size = new Size(150, 25);
            txtPassword.PasswordChar = '*';
            this.Controls.Add(txtPassword);

            // Кнопка Войти
            btnLogin = new Button();
            btnLogin.Text = "Войти";
            btnLogin.Location = new Point(50, 100);
            btnLogin.Click += BtnLogin_Click;
            this.Controls.Add(btnLogin);

            // Кнопка Регистрация
            btnRegister = new Button();
            btnRegister.Text = "Регистрация";
            btnRegister.Location = new Point(150, 100);
            btnRegister.Size = new Size(100, 30);
            btnRegister.Click += BtnRegister_Click;
            this.Controls.Add(btnRegister);
        }

        private void LoadUsers()
        {
            if (File.Exists(usersFile))
            {
                string json = File.ReadAllText(usersFile);
                users = JsonSerializer.Deserialize<Dictionary<string, string>>(json);
            }
        }

        private void SaveUsers()
        {
            string json = JsonSerializer.Serialize(users);
            File.WriteAllText(usersFile, json);
        }

        private void BtnLogin_Click(object sender, EventArgs e)
        {
            string login = txtLogin.Text;
            string password = txtPassword.Text;

            if (users.ContainsKey(login) && users[login] == password)
            {
                this.Hide();
                BooksForm books = new BooksForm(login);
                books.ShowDialog();
                this.Close();
            }
            else
            {
                MessageBox.Show("Неверный логин или пароль!");
            }
        }

        private void BtnRegister_Click(object sender, EventArgs e)
        {
            string login = txtLogin.Text;
            string password = txtPassword.Text;

            if (users.ContainsKey(login))
            {
                MessageBox.Show("Пользователь уже есть!");
                return;
            }

            users[login] = password;
            SaveUsers();
            MessageBox.Show("Регистрация успешна!");
        }
    }
}
```

---

## ШАГ 5: BooksForm.cs (форма с книгами)

```csharp
using System;
using System.Collections.Generic;
using System.Drawing;
using System.IO;
using System.Text.Json;
using System.Windows.Forms;

namespace CollegeLibrary
{
    public class BooksForm : Form
    {
        private string currentUser;
        private List<string> books = new List<string>();
        private string fileName = "books.json";

        private TextBox txtBook;
        private Button btnAdd;
        private Button btnDelete;
        private ListBox listBooks;

        public BooksForm(string username)
        {
            currentUser = username;
            CreateElements();
            LoadData();
        }

        private void CreateElements()
        {
            this.Text = "Библиотека - " + currentUser;
            this.Size = new Size(400, 350);

            // Поле ввода
            txtBook = new TextBox();
            txtBook.Location = new Point(10, 10);
            txtBook.Size = new Size(250, 25);
            this.Controls.Add(txtBook);

            // Кнопка Добавить
            btnAdd = new Button();
            btnAdd.Text = "Добавить";
            btnAdd.Location = new Point(270, 10);
            btnAdd.Click += BtnAdd_Click;
            this.Controls.Add(btnAdd);

            // Кнопка Удалить
            btnDelete = new Button();
            btnDelete.Text = "Удалить";
            btnDelete.Location = new Point(270, 45);
            btnDelete.Click += BtnDelete_Click;
            this.Controls.Add(btnDelete);

            // Список книг
            listBooks = new ListBox();
            listBooks.Location = new Point(10, 45);
            listBooks.Size = new Size(250, 250);
            this.Controls.Add(listBooks);
        }

        private void LoadData()
        {
            if (File.Exists(fileName))
            {
                string json = File.ReadAllText(fileName);
                books = JsonSerializer.Deserialize<List<string>>(json);
                if (books == null) books = new List<string>();
            }
            UpdateList();
        }

        private void SaveData()
        {
            string json = JsonSerializer.Serialize(books);
            File.WriteAllText(fileName, json);
        }

        private void UpdateList()
        {
            listBooks.Items.Clear();
            foreach (string book in books)
            {
                listBooks.Items.Add(book);
            }
        }

        private void BtnAdd_Click(object sender, EventArgs e)
        {
            if (txtBook.Text != "")
            {
                books.Add(txtBook.Text);
                SaveData();
                UpdateList();
                txtBook.Text = "";
            }
        }

        private void BtnDelete_Click(object sender, EventArgs e)
        {
            if (listBooks.SelectedIndex >= 0)
            {
                books.RemoveAt(listBooks.SelectedIndex);
                SaveData();
                UpdateList();
            }
        }
    }
}
```

---

## ШАГ 6: Запуск

Нажимаем **F5** - готово!

---

---

# ШПАРГАЛКА

## Using-и (копируй в начало каждого файла):
```csharp
using System;
using System.Collections.Generic;
using System.Drawing;
using System.IO;
using System.Text.Json;
using System.Windows.Forms;
```

## Создать форму (новое окно):
```csharp
public class МояФорма : Form
{
    public МояФорма()
    {
        this.Text = "Заголовок";
        this.Size = new Size(400, 300);
    }
}
```

## Открыть другую форму:
```csharp
this.Hide();                    // скрыть текущую
НоваяФорма form = new НоваяФорма();
form.ShowDialog();              // показать новую
this.Close();                   // закрыть текущую
```

## TextBox для пароля (звёздочки):
```csharp
txtPassword.PasswordChar = '*';
```

## Dictionary (словарь) - для хранения логин:пароль:
```csharp
Dictionary<string, string> users = new Dictionary<string, string>();

users["ivan"] = "12345";              // добавить
bool есть = users.ContainsKey("ivan"); // проверить есть ли
string пароль = users["ivan"];         // получить
```

## Сохранить/загрузить Dictionary:
```csharp
// Сохранить
File.WriteAllText("users.json", JsonSerializer.Serialize(users));

// Загрузить
users = JsonSerializer.Deserialize<Dictionary<string, string>>(File.ReadAllText("users.json"));
```

## Элементы формы:
```csharp
// TextBox (поле ввода)
TextBox txt = new TextBox();
txt.Location = new Point(100, 20);
txt.Size = new Size(150, 25);
this.Controls.Add(txt);

// Button (кнопка)
Button btn = new Button();
btn.Text = "Нажми";
btn.Location = new Point(100, 50);
btn.Click += Btn_Click;
this.Controls.Add(btn);

// Label (надпись)
Label lbl = new Label();
lbl.Text = "Текст:";
lbl.Location = new Point(10, 20);
this.Controls.Add(lbl);

// ListBox (список)
ListBox list = new ListBox();
list.Location = new Point(10, 50);
list.Size = new Size(200, 200);
this.Controls.Add(list);
```

## Работа с ListBox:
```csharp
list.Items.Add("элемент");           // добавить
list.Items.Clear();                   // очистить
int i = list.SelectedIndex;           // номер выбранного (-1 если нет)
```

## MessageBox:
```csharp
MessageBox.Show("Сообщение!");
```

---

# УДАЧИ НА ЭКЗАМЕНЕ!
